services:

  status-service:
    build:
      context: .
      dockerfile: Dockerfile

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - status-service